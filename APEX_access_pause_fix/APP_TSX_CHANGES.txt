APEX access-pause integration — App.tsx

After replacing the two supplied files, make these exact small edits in src/App.tsx.

1) In the values returned from useChampionshipData(), add these two lines directly below cooldownSeconds:

    isPublicAccessRestricted,
    sourceAccessRetrySeconds,

2) In autoSyncStatus, add this condition BEFORE the cooldown condition:

    if (isPublicAccessRestricted && sourceAccessRetrySeconds > 0) {
      return 'paused-source-access';
    }

3) Add isPublicAccessRestricted and sourceAccessRetrySeconds to the dependency array below autoSyncStatus.

4) In autoSyncLabel, add this condition BEFORE paused-rate-limit:

    if (autoSyncStatus === 'paused-source-access') {
      return `AUTO-SYNC PAUSED · PUBLIC ACCESS RETRY IN ${String(
        Math.floor(sourceAccessRetrySeconds / 60)
      ).padStart(2, '0')}:${String(sourceAccessRetrySeconds % 60).padStart(2, '0')}`;
    }

5) In EVERY DataIntegrityPanel component (there are three), find:

    cooldownSeconds={cooldownSeconds}

Add these two lines directly below it:

    isPublicAccessRestricted={isPublicAccessRestricted}
    sourceAccessRetrySeconds={sourceAccessRetrySeconds}

6) Save all files, then run:

    npm run build

Result:
- Auto-sync pauses during the provider restriction instead of sending repeated refreshes.
- Force Live Re-Sync and Resume Missing Rounds clearly display an amber countdown instead of looking frozen.
- The hook returns quickly while the retry timer runs, so the UI does not remain stuck in a loading state.
